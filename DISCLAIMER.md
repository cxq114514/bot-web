# 使用说明与免责声明

Crawlspace 是可配置的数据采集工具。使用前，请确认目标网页或接口的访问权限、允许用途和使用规则。应用中的 `/disclaimer` 页面也可查看本说明。

1. **访问授权**：仅用于你有权访问的网页和接口。公开可访问并不等于允许任意采集、复制、传播或商用。请核对服务条款、API 协议和适用要求。不要绕过登录、付费限制、验证码、访问控制或其他技术保护措施。
2. **规则和权益**：工具检查 robots.txt 并限制频率，但 robots.txt 不是版权、隐私或使用授权。采集内容的权益属于相关权利人；请遵循许可、署名和用途范围等要求，避免不必要的个人信息、敏感信息或受保护数据采集。
3. **自定义接口**：GET／POST 请求都需要授权。POST 可能修改远端数据，只应配置明确获准的只读查询接口。请核对请求体，不使用他人凭据。
4. **凭据处理**：自定义接口的请求头、请求体和 URL 查询参数不随规则保存，也不主动加入应用采集日志；它们会经过本工具的运行服务器发送到目标接口。部署平台、代理和目标服务可能另行记录请求，请核对其配置和数据处理规则。HTML 模式的 URL 会随规则和来源保存，请勿在其中填写密钥；任何模式都不要将密钥放在 URL 路径中。返回数据可能含敏感信息，导出前应自行检查。
5. **本地数据和部署**：站点规则保存在当前浏览器，不跨设备同步；结果在页面内存中，刷新即丢失。请保管导出文件并清理共享设备上的存储。项目默认作为个人本地工具，不自带账户鉴权或分布式限流。公开部署需配置访问控制、资源限额和出站访问策略。
6. **结果完整性**：页面结构变化、动态内容、字段未匹配、网络失败、手动停止及页数、记录数、大小上限都可能导致缺失或截断。“采集完成”只表示本次流程结束，不保证已采集目标全部数据。请结合警告、原始来源和实际用途核验；中断后导出的只是已收到的部分结果。
7. **可用性和责任**：工具按现状提供，不承诺持续可用或结果准确、完整、适合特定用途。操作者应核对采集及后续使用权限，部署者应管理访问控制、日志和凭据安全；重要数据请独立验证并备份。本说明不授予第三方数据使用权，不替代具体授权，也不排除适用规则下依法不得排除的责任。

频率限制仅在同一 Node.js 进程内按来源共享，不协调多个实例或其他采集工具；部署者应根据目标服务要求进一步限制总请求量。

无法确定采集是否被允许时，请先联系目标网站或接口的管理方。
